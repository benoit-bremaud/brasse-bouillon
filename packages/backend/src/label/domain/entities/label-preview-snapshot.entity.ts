/**
 * LabelPreviewSnapshot
 *
 * Persisted preview payload used to quickly display draft history.
 *
 * The shape stays intentionally open for MVP flexibility.
 */
export type LabelPreviewSnapshot = Record<string, unknown>;
